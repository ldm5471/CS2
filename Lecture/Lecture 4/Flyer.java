package inheritance;

/**
 * A Flyer can fly a certain distance.
 * @author CS RIT
 */
public interface Flyer {

    /**
     * Fly for some distance.
     *
     * @param distance amount to fly.
     */
    void fly(int distance);
}
